# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# Kotlin
.kotlin/

# Kotlin/JS — the yarn lockfile SHOULD be committed for reproducible JS builds,
# so it is deliberately NOT ignored here (see kotlin.js.yarn in gradle.properties).

# Local / IDE
local.properties
.idea/
*.iml
.DS_Store

# OS / editor
*.swp
*~

# The three.js reference checkout used while porting. It is not part of Kite3D
# and is never committed; see CONTRIBUTING.md for how to obtain it.
three.js-ref/
